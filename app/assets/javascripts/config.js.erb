window.Aker = {
  config: {
    materials_root: '<%= Rails.configuration.materials_root %>',
    study_root: '<%= Rails.configuration.study_root %>',
    sets_root: '<%= Rails.configuration.sets_root %>'
  }
}